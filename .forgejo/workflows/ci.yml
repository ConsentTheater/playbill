name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: codeberg-small
    container:
      image: node:24
    steps:
      - uses: actions/checkout@v6
      - run: npm ci
      - run: npm run build
      - run: npx vitest run --passWithNoTests
